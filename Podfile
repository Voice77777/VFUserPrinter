# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VFUserPrinter' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'SwiftyBeaver'
  # Pods for VFUserPrinter

  target 'VFUserPrinterTests' do
    # Pods for testing
  end

end
